# Ghi chú trích xuất tạm — Ngày 23

**Nguồn:** PDF FILE ĐỀ Drive ID `1JGEOYNq1t5eDl8WotDAFfVFF_l7WQINB`, PDF đề online ID `10v9WBBMvqODwPnnUG1OqLHpswtsaY8vc`, BÀI HỌC YouTube chưa transcript. Artifact text: `day-23-lesson.txt` và `day-23-online.txt`.

PDF lý thuyết ghi `UNIT 23: LIÊN TỪ AND BUT OR SO VÀ BECAUSE`. Từ vựng gồm `catch`, `laugh`, `prefer`; tính từ `rich`, `unhappy`, `sick`, `ill`, `chocolate`, `plane`, `basketball`, `fan`, `jacket`, `pizza`, `black`, `white`; trạng từ `heavily`, `extremely`, kèm phiên âm nguồn.

Phần Grammar: `and` nối hai thành phần cùng loại với nghĩa và; `but` nối thành phần cùng loại để thể hiện trái ngược; `or` thể hiện lựa chọn; `so` nối hai mệnh đề chỉ kết quả; `because` đứng trước mệnh đề chỉ lý do, có thể ở đầu hoặc giữa câu, và không dùng đồng thời với `so`. Ví dụ nguồn gồm `He is rich but he is unhappy.`, `Do you want chocolate or ice cream?`, `It rained heavily so I was late.`, `Because it rained heavily, I couldn’t catch the bus.`.

PDF có Quiz 4 MCQ 2 lựa chọn và Practice 15 MCQ 2 lựa chọn. Đề online cần đối chiếu riêng trước khi quyết định ánh xạ; theo phạm vi hiện hành chưa đưa quiz chính thức vào `days.json` nếu chưa có schema được xác nhận.
